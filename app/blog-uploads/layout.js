export const metadata = {
  title: 'Blog Management',
  robots: {
    index: false,
    follow: false,
    googleBot: {
      index: false,
      follow: false,
    },
  },
};

export default function BlogUploadsLayout({ children }) {
  return children;
}
