export const metadata = {
  title: "Leads",
  robots: {
    index: false,
    follow: false,
    googleBot: {
      index: false,
      follow: false,
      noimageindex: true,
    },
  },
};

export default function PrivateLayout({ children }) {
  return children;
}
